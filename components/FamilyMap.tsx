'use client';
import { MapContainer, Marker, Popup, TileLayer } from 'react-leaflet'; import L from 'leaflet'; import type { Location } from '@/types/database';
const icon=(name:string,color:string)=>L.divIcon({className:'',html:`<div class="map-pin" style="width:44px;height:44px;background:${color};display:grid;place-items:center;color:#fff;font-weight:bold">${name.slice(0,1).toUpperCase()}</div>`,iconSize:[44,44],iconAnchor:[22,22]});
export function FamilyMap({locations,onChat}:{locations:Location[],onChat:(id:string)=>void}){const center: [number,number]=locations[0]?[locations[0].latitude,locations[0].longitude]:[41.3111,69.2797];return <MapContainer center={center} zoom={14} scrollWheelZoom className="h-full"><TileLayer attribution="&copy; OpenStreetMap contributors" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/>{locations.map((l,i)=><Marker key={l.user_id} position={[l.latitude,l.longitude]} icon={icon(l.user?.display_name||'?', ['#377DFF','#ef6b73','#7b61ff','#30a46c'][i%4])}><Popup><div className="min-w-40"><b>{l.user?.display_name}</b><p className="my-1 text-xs">{l.user?.online?'🟢 Онлайн':'⚪ Не в сети'}</p><p className="text-xs">Обновлено {new Date(l.updated_at).toLocaleTimeString('ru',{hour:'2-digit',minute:'2-digit'})}</p><button onClick={()=>onChat(l.user_id)} className="mt-2 rounded bg-blue-500 px-2 py-1 text-xs text-white">Написать</button></div></Popup></Marker>)}</MapContainer>}
